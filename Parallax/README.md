# Parallax

`SourceArt` contains Moonlight City, Neon Alley, and Rooftops 2 layers. The editable campaign prototypes already configure these as Far, Middle, and Front `Parallax2D` nodes. Textures can also be loaded by relative path with `AssetRegistry.get_parallax_texture()`.
