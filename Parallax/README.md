# Parallax

`SourceArt` contains Moonlight City, Neon Alley, and Rooftops 2 layers. Production campaign stages configure Far, Middle, and Front `Parallax2D` nodes; their stage-specific hero panoramas and layer treatments are recorded under `docs/assets/`. Textures can also be loaded by relative path with `AssetRegistry.get_parallax_texture()`.
